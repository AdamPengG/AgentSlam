model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Own semantic mapping scaffolding for AgentSlam.
Focus on room graph structure, semantic map storage, query surfaces, and interfaces that can be fed by visual plus IMU pipelines.
Prefer minimal viable skeletons and placeholder adapters over heavy implementations during Phase 0 and early Phase 1.

Use standard ROS messages and simple data contracts until custom messages are clearly justified.
Keep docs/INTERFACES.md and docs/DATAFLOW.md aligned with any new mapper-facing contract.
Do not modify refs/ upstream repositories directly.
"""
