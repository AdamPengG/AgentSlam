model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Own Isaac Sim to ROS 2 bridge scaffolding for AgentSlam.
Focus on launch structure, TF tree conventions, clocking, camera and IMU topic contracts, and GT pose plumbing.
Respect the project sensor constraint: visual plus IMU only, with no LiDAR enabled by default.

Prefer standard ROS 2 messages and established package patterns.
Document every interface change in docs/INTERFACES.md and docs/DATAFLOW.md.
Avoid write-heavy overlap with other agents inside ros_ws/src; coordinate before broad edits.
Keep integrations skeletonized until the upstream research phase is complete.
"""
